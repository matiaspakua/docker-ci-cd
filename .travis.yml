sudo: required
services:
  - docker

scripts:
  - docker build -t mannydesigns/node .
  - docker images mannydesigns/node

before_deploy:
  - docker login -u matiasmiguez -p 31552083Matias

deploy:
  provider: script
  script: docker push matiasmiguez/docker-travis-ci
  on:
    branch: master